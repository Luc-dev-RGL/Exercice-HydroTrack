<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème de base pour l'activité - sans barre d'action car nous utilisons Jetpack Compose -->
    <style name="Theme.HydroTrack" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.TempProject" parent="android:Theme.Material.Light.NoActionBar" />
</resources>